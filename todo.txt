* Load models
	* Update TM to properly import and STL
		* Look for pre-existing STL parsers
			* Adapt those to fit TM
			* Get homers face to appear
		* Parse file formats that blender supports
			* Look for pre-existing parsers
	* Import the teapot
	* Add support for texture data / color in TM
		* Make it toggle-able
* Get user input to move the perspective
	* Toggle using the mouse to look around/
		dragging the focused object around
* Scale the model automatically, based on the farthest point from the center
* Do some cloth simulation
	* Add gravity
	* Add a cloth class
	* Allow segments to be deleted on the fly

